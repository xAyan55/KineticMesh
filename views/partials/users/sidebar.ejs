<link rel="stylesheet" href="/css/admin.css">
<aside class="admin-sidebar" id="adminSidebar">
    
    <!-- Brand -->
    <a href="/dashboard" class="sidebar-brand">
        <div class="sidebar-brand-icon" style="display: flex; align-items: center; justify-content: center; width: 40px; height: 40px; border-radius: var(--radius-md); overflow: hidden; background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
          <img src="<%= siteLogo %>" style="width: 100%; height: 100%; border-radius: var(--radius-md); object-fit: cover; padding: 2px;" alt="<%= siteName %> Logo">
        </div>
        <div class="sidebar-brand-text">
            <div class="sidebar-brand-name" id="userSidebarName"><%= siteName %></div>
            <div class="sidebar-brand-version"><%= panelVersion %> User</div>
        </div>
    </a>
    
    <nav class="sidebar-nav">
        <div class="nav-section">
            <div class="nav-section-label">Menu</div>
            
            <a href="/dashboard" class="nav-item <%= currentPage === 'dashboard' ? 'active' : '' %>">
                <span class="nav-icon">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <rect x="3" y="3" width="7" height="9" rx="1"></rect>
                        <rect x="14" y="3" width="7" height="5" rx="1"></rect>
                        <rect x="14" y="12" width="7" height="9" rx="1"></rect>
                        <rect x="3" y="16" width="7" height="5" rx="1"></rect>
                    </svg>
                </span>
                <span class="nav-text">Dashboard</span>
            </a>
            
            <a href="/vms" class="nav-item <%= currentPage === 'vms' ? 'active' : '' %>">
                <span class="nav-icon">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <rect x="2" y="4" width="20" height="16" rx="2"></rect>
                        <path d="M8 20h8"></path>
                        <path d="M12 16v4"></path>
                        <circle cx="12" cy="12" r="3"></circle>
                    </svg>
                </span>
                <span class="nav-text">My Virtual Machines</span>
            </a>
            
            <a href="/profile" class="nav-item <%= currentPage === 'profile' ? 'active' : '' %>">
                <span class="nav-icon">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                        <circle cx="12" cy="7" r="4"></circle>
                    </svg>
                </span>
                <span class="nav-text">My Profile</span>
            </a>
        </div>
        
        <!-- Admin Section (shown only to admins) -->
        <% if (user && user.role === 'admin') { %>
        <div class="nav-section">
            <div class="nav-section-label">Administration</div>
            
            <a href="/admin/dashboard" class="nav-item nav-item-highlight">
                <span class="nav-icon">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M12 15a7 7 0 1 0 0-14 7 7 0 0 0 0 14z"></path>
                        <path d="M8.21 13.89L7 23l5-3 5 3-1.21-9.12"></path>
                    </svg>
                </span>
                <span class="nav-text">Admin Panel</span>
                <span class="nav-badge nav-badge-admin">ADMIN</span>
            </a>
        </div>
        <% } %>
    </nav>
    
    <div class="sidebar-footer">
        <div class="sidebar-user" onclick="toggleDropdown('userSidebarMenu')">
            <div class="user-avatar" id="userSidebarAvatar" style="background-size: cover; background-position: center; position: relative;">
                <span id="userSidebarInitial"><%= user ? user.username.charAt(0).toUpperCase() : 'U' %></span>
                <span id="userSidebarDiscordBadge" style="position: absolute; bottom: -2px; right: -2px; width: 14px; height: 14px; background: #5865F2; border-radius: 50%; display: none; border: 2px solid var(--bg-primary);"></span>
            </div>
            <div class="user-info">
                <div class="user-name"><%= user ? user.username : 'Guest' %></div>
                <div class="user-role">
                    <span style="display: inline-flex; align-items: center; gap: 4px;">
                        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                            <circle cx="12" cy="7" r="4"></circle>
                        </svg>
                        User
                    </span>
                </div>
            </div>
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="color: var(--text-muted); flex-shrink:0;">
                <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
        </div>

        <div class="dropdown-menu" id="userSidebarMenu" style="bottom: 100%; top: auto; margin-bottom: 6px; right: 0; left: 0; min-width: unset;">
            <button class="dropdown-item danger" onclick="userLogout()">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                    <polyline points="16 17 21 12 16 7"></polyline>
                    <line x1="21" y1="12" x2="9" y2="12"></line>
                </svg>
                Sign Out
            </button>
        </div>
    </div>
</aside>

<div class="sidebar-overlay" id="sidebarOverlay" onclick="closeSidebar()"></div>

<script>
    // Handle user logout
    async function userLogout() {
        try {
            const response = await fetch('/api/logout', { 
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                }
            });
            if (response.ok) {
                window.location.href = '/';
            } else {
                // Force redirect even on error
                window.location.href = '/';
            }
        } catch (error) {
            console.error('[Logout Error]', error);
            window.location.href = '/';
        }
    }
    
    // Load site name from API on page load to refresh
    async function loadUserSiteName() {
        try {
            const res = await fetch('/api/settings');
            if (!res.ok) return;
            const settings = await res.json();
            const siteName = settings['site_name'] || '<%= siteName %>';
            const nameEl = document.getElementById('userSidebarName');
            if (nameEl) {
                nameEl.textContent = siteName;
            }
        } catch(e) {
            console.error('Failed to load site name:', e);
        }
    }
    
    // Sidebar toggle functions
    function toggleSidebar() {
        const sidebar = document.getElementById('adminSidebar');
        const overlay = document.getElementById('sidebarOverlay');
        sidebar.classList.toggle('open');
        overlay.classList.toggle('open');
        document.body.style.overflow = sidebar.classList.contains('open') ? 'hidden' : '';
    }
    
    function closeSidebar() {
        const sidebar = document.getElementById('adminSidebar');
        const overlay = document.getElementById('sidebarOverlay');
        sidebar.classList.remove('open');
        overlay.classList.remove('open');
        document.body.style.overflow = '';
    }
    
    // Load site name on page load
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', loadUserSiteName);
    } else {
        loadUserSiteName();
    }
    
    // Auto-refresh site name every 30 seconds
    setInterval(loadUserSiteName, 30000);

    // === LOAD DISCORD AVATAR ===
    async function loadUserSidebarAvatar() {
      try {
        console.log('[DEBUG] loadUserSidebarAvatar called');
        
        const avatar = document.getElementById('userSidebarAvatar');
        console.log('[DEBUG] userSidebarAvatar element:', avatar);
        
        if (!avatar) {
          console.error('[DEBUG] userSidebarAvatar element not found');
          return;
        }
        
        const res = await fetch('/api/discord-account-info');
        console.log('[DEBUG] Discord account info response:', res.ok);
        
        if (res.ok) {
          const discordAccount = await res.json();
          console.log('[DEBUG] Discord account data:', discordAccount);
          
          if (discordAccount && discordAccount.discord_avatar && discordAccount.discord_id) {
            // Build Discord CDN avatar URL with hash
            const discordAvatarUrl = `https://cdn.discordapp.com/avatars/${discordAccount.discord_id}/${discordAccount.discord_avatar}.png?size=128`;
            console.log('[DEBUG] Discord avatar URL:', discordAvatarUrl);
            
            const initial = document.getElementById('userSidebarInitial');
            const badge = document.getElementById('userSidebarDiscordBadge');
            
            console.log('[DEBUG] Initial element:', initial);
            console.log('[DEBUG] Badge element:', badge);
            
            // Set background image
            avatar.style.backgroundImage = `url('${discordAvatarUrl}')`;
            avatar.style.backgroundSize = 'cover';
            avatar.style.backgroundPosition = 'center';
            
            // Hide the initial text
            if (initial) {
              initial.style.display = 'none';
              console.log('[DEBUG] Hidden initial text');
            }
            
            // Show Discord badge
            if (badge) {
              badge.style.display = 'block';
              console.log('[DEBUG] Showing Discord badge');
            }
            
            console.log('[DEBUG] Discord avatar applied successfully!');
          } else {
            console.log('[DEBUG] No Discord avatar data:', { avatar: discordAccount?.discord_avatar, id: discordAccount?.discord_id });
          }
        } else {
          console.log('[DEBUG] Discord account info fetch not ok:', res.status);
        }
      } catch(e) {
        console.error('[Discord Avatar Error]', e);
      }
    }

    // Call on page load with multiple triggers
    document.addEventListener('DOMContentLoaded', () => {
      setTimeout(loadUserSidebarAvatar, 300);
    });

    window.addEventListener('load', () => {
      setTimeout(loadUserSidebarAvatar, 300);
    });

    // If page is already loaded
    if (document.readyState === 'complete' || document.readyState === 'interactive') {
      setTimeout(loadUserSidebarAvatar, 500);
    }
</script>